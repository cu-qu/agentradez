[build]
builder = "RAILPACK"

[deploy]
preDeployCommand = ["bash ./build.sh"]
startCommand = "python -m gunicorn config.wsgi:application --bind 0.0.0.0:${PORT:-8000}"
healthcheckPath = "/api/health/"
healthcheckTimeout = 300
restartPolicyType = "ALWAYS"
