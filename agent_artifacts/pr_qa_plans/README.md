# PR QA plans

One markdown file per pull request (or per release candidate).

Suggested filename: `YYYY-MM-DD-pr-short-title.md`

Include:

- What to click / call
- Auth states to cover (logged out, verified, unpaid, paid)
- API + admin checks
- Mobile/web matrix if both clients exist
- Pass / fail notes
